<odoo>

    <!-- TREE VIEW -->
    <record id="view_hr_location_tree" model="ir.ui.view">
        <field name="name">hr.location.tree</field>
        <field name="model">hr.location</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <!-- FORM VIEW -->
    <record id="view_hr_location_form" model="ir.ui.view">
        <field name="name">hr.location.form</field>
        <field name="model">hr.location</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ACTION -->
    <record id="action_hr_location" model="ir.actions.act_window">
        <field name="name">Location Master</field>
        <field name="res_model">hr.location</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_hr_master_root"
              name="Master"
              parent="hr_recruitment.menu_hr_recruitment_configuration"
              sequence="50"/>

    <!-- SUB MENU -->
    <menuitem id="menu_hr_location"
              name="Locations"
              parent="bxi_hr_recruitment.menu_hr_master_root"
              action="bxi_hr_recruitment.action_hr_location"
              sequence="1"/>

</odoo>